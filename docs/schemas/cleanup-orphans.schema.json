{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/daniloaguiar/neurographrag/schemas/cleanup-orphans.schema.json",
  "title": "CleanupOrphansResponse",
  "description": "Response emitted by `neurographrag cleanup-orphans` on stdout as a single JSON line.",
  "type": "object",
  "required": ["orphan_count", "deleted", "dry_run", "namespace"],
  "additionalProperties": false,
  "properties": {
    "orphan_count": { "type": "integer", "minimum": 0, "description": "Total orphan entities found." },
    "deleted":      { "type": "integer", "minimum": 0, "description": "Entities deleted (0 when dry_run=true)." },
    "dry_run":      { "type": "boolean" },
    "namespace":    { "type": "string" }
  }
}
