{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/daniloaguiar/neurographrag/schemas/init.schema.json",
  "title": "InitResponse",
  "description": "Response emitted by `neurographrag init` on stdout as a single JSON line.",
  "type": "object",
  "required": ["db_path", "schema_version", "model", "dim", "namespace", "status"],
  "additionalProperties": false,
  "properties": {
    "db_path":        { "type": "string", "description": "Absolute path to the SQLite database file." },
    "schema_version": { "type": "integer", "description": "SQLite PRAGMA user_version after migration." },
    "model":          { "type": "string", "description": "Embedding model name downloaded or reused." },
    "dim":            { "type": "integer", "description": "Embedding vector dimension (e.g. 384)." },
    "namespace":      { "type": "string", "description": "Active namespace resolved at init time." },
    "status":         { "type": "string", "enum": ["created", "already_exists"], "description": "Whether the database was newly created or already existed." }
  }
}
