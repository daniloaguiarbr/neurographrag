{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/daniloaguiar/neurographrag/schemas/forget.schema.json",
  "title": "ForgetResponse",
  "description": "Response emitted by `neurographrag forget` on stdout as a single JSON line.",
  "type": "object",
  "required": ["forgotten", "name", "namespace"],
  "additionalProperties": false,
  "properties": {
    "forgotten": { "type": "boolean", "description": "True when the memory was soft-deleted." },
    "name":      { "type": "string" },
    "namespace": { "type": "string" }
  }
}
